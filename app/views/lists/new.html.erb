<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.input :image_url, placeholder: "Entrez l'URL de votre image" %>
  <%= f.submit "Créer la liste", class: "btn btn-primary" %>
<% end %>
